# Regex

Use `arkregex`'s `regex(...)` when a pattern has capture groups whose results are read in code — it types them. For patterns without consumed captures, use a plain regex literal. For dynamic literal text, use `RegExp.escape`.

Run `/arkregex` when authoring or migrating capture-group regexes.

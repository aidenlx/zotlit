# @zotlit/website

## Post-scaffold setup):

```bash
pnpm dlx @tanstack/intent@latest install   # writes the intent-skills block in AGENTS.md
pnpm dlx @tanstack/intent@latest list      # discovers @tanstack skills bundled with deps
```

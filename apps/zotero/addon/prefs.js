// Default values for `extensions.zotlit.*` prefs. Auto-loaded by Zotero from
// the XPI root. Read at runtime via the typed wrapper in `src/prefs/index.ts`.
//
// Keep prefixes literal (`extensions.zotlit.*`) — we don't have a build-time
// prefixing pipeline (see DEFERRED.md).
//

pref("extensions.zotlit.notify", false);
pref("extensions.zotlit.notify-url", "http://localhost:9091");
pref("extensions.zotlit.log.console-level", "warning");

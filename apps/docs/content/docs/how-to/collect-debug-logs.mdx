---
title: "Collect debug logs"
description: "Gather a template error report, ZotLit logs, Zotero debug output, and Obsidian debug info to include when reporting a bug."
introduced: "2.0.0"
updated: "2.1.0"
---

import { Steps, Step } from "fumadocs-ui/components/steps";
import { Card, Cards } from "fumadocs-ui/components/card";
import { Callout } from "@/components/callout";
import { Command } from "@/components/command";
import { SettingsPath } from "@/components/settings-path";
import { UiLabel } from "@/components/ui-label";
import { m } from "@/paraglide/messages.js";

Bug reports often need a template error report, ZotLit logs, Obsidian logs, or Zotero debug output from ZotLit Companion, the Zotero add-on. Each section below is self-contained. Follow only the one you were asked for.

Once you have the logs, file the report in the [Bug reports discussion](https://github.com/aidenlx/zotlit/discussions/categories/bug-reports).

<Callout type="warn" title="Cross-component bugs need both sides">
  If the bug spans ZotLit and the Companion (e.g. syncing, live update,
  importing, or any action that rely on Zotero running), complete the setup
  steps in **both** [ZotLit logs](#zotlit-logs) and [Zotero debug
  output](#zotero-debug-output) before you reproduce the problem. Reproduce
  once, then save logs from both sides.
</Callout>

## Template error report [#template-error-report]

Collect this when a template preview fails in the Template Workbench. The report holds the template engine's own message and the context of the attempt that failed, so another person can investigate the actual failure.

<Steps>
  <Step>
    ### Open the problem

    In the Template Workbench, select <UiLabel name={m.workbench_problem_show()} /> in the preview or on the source marker. The <UiLabel name={m.workbench_problems_heading()} /> area explains the failure.

  </Step>
  <Step>
    ### Copy the report

    Select <UiLabel name={m.workbench_problems_copy()} />. Select <UiLabel name={m.workbench_problems_technical()} /> first if you want to read the report before you copy it: the disclosure shows the same text.

    The report holds the engine message, the error name, its causes, any source excerpt, and the location the engine named. Beside those it records the time of the failure, whether an automatic check or <UiLabel name={m.workbench_preview_run()} /> started it, the Template Document and section, the template language, the preview root and its options, the selected item or example, the source revision, and the ZotLit and Obsidian versions. A value the failed attempt could not supply reads `unavailable`.

  </Step>
  <Step>
    ### Paste it into the report

    Paste the copied text into your bug report. Nothing leaves your vault until you paste it yourself.

  </Step>
</Steps>

<Callout type="info" title="The report survives the repair">
  Keep working on the template. The copied report describes the attempt that
  failed, so later edits and a different selected item do not change it. After a
  successful repair, the open <UiLabel name={m.workbench_problems_heading()} />{" "}
  area offers <UiLabel name={m.workbench_problems_copy_last()} /> for the
  failure you were reading.
</Callout>

<Callout type="info" title="Ask the community">
  <UiLabel name={m.workbench_problems_community()} /> sits beside the copy
  action and opens the [community page](/community). It opens a page; it sends
  nothing.
</Callout>

### When to add more evidence

The copied report covers one failed template attempt. Collect one of the following as well when it applies:

- **[ZotLit logs](#zotlit-logs)** — the failure has no engine message, or it happens while creating or updating a note rather than in the preview.
- **[Template data](#template-data)** — the template rendered without failing, but produced the wrong content.
- **[Obsidian debug info](#obsidian-debug-info)** — the problem may involve another plugin, a theme, or a specific Obsidian build.

Each one is a separate collection step. The copied report never carries a log archive or a template-data export.

## Obsidian debug info [#obsidian-debug-info]

<Steps>
  <Step>
    ### Run the debug info command

    Open the command palette and run <Command inline>Show debug info</Command>.

  </Step>
  <Step>
    ### Copy the output

    Copy the output and paste it into the issue.

  </Step>
</Steps>

## ZotLit logs

<Steps>
  <Step>
    ### Open the Advanced settings

    In Obsidian, open <SettingsPath page={m.settings_page_advanced()} /> and find the <UiLabel name={m.settings_advanced_logging_heading()} /> group.

  </Step>
  <Step>
    ### Turn on debug logging

    Set <UiLabel name={m.settings_log_level_name()} /> to **debug**, then turn on <UiLabel name={m.settings_log_to_file_name()} />. Logs now append to `zotlit.log.jsonl` inside the plugin folder.

  </Step>
  <Step>
    ### Reproduce the problem

    Do whatever triggers the bug.

  </Step>
  <Step>
    ### Save the log archive

    Back in <SettingsPath page={m.settings_page_advanced()} />, click <UiLabel name={m.settings_log_export_name()} />. This exports the log file for sharing.

  </Step>
  <Step>
    ### Upload the archive

    Attach the saved archive to your bug report.

  </Step>
</Steps>

<Callout type="info" title="Turn logging back off">
  Set <UiLabel name={m.settings_log_level_name()} /> back to **info** and turn
  off
  <UiLabel name={m.settings_log_to_file_name()} /> once you no longer need
  ongoing logging.
</Callout>

## Template data [#template-data]

Collect this when a template rendered the wrong thing. The saved file holds the exact data your templates received for one item.

<Steps>
  <Step>
    ### Open the explorer on the item

    [Open the Template Data Explorer](/docs/how-to/explore-template-data) on the item that reproduces the problem.

  </Step>
  <Step>
    ### Anchor at the annotation

    If the problem is in an annotation template, open the more-options menu on that annotation node in the tree and select <UiLabel name={m.template_data_explorer_menu_explore_annotation()} />. For any other template, skip this step and stay at the note root.

  </Step>
  <Step>
    ### Save the template data

    Open the more-options menu on the explorer's tab and select <UiLabel name={m.template_data_explorer_menu_export_json()} />. This downloads a file named like `zotlit-template-data-ABCD2345-20260512-143000.json`.

    The file always holds the whole current root: the full note template data at the note root, or the annotation's data when the explorer is anchored at an annotation. A filter in the explorer does not narrow it.

  </Step>
  <Step>
    ### Upload the file

    Attach the saved file to your bug report.

  </Step>
</Steps>

<Callout type="warn" title="Read the file before you share it">
  The file contains Zotero library metadata for the item, absolute paths to
  attachments inside your Zotero data folder, and your Zotero account username.
  Review it before you attach it to a public bug report.
</Callout>

<Callout type="info" title="Placeholder markers are expected">
  The explorer never writes files into your vault, so helpers that normally
  create them, such as annotation excerpt images and imported child notes,
  produce placeholder markers instead of real links. These appear as `$inert`
  entries in the file, and helpers appear as `$helper` entries. Both are
  expected in the saved data.
</Callout>

## Zotero debug output

<Steps>
  <Step>
    ### Set the Companion's log level

    In Zotero, open **Edit > Settings** (Windows/Linux) or **Zotero > Settings** (macOS), then select the <UiLabel name={m["zotero.prefs_pane_label"]()} /> pane. Set <UiLabel name={m["zotero.prefs_logging_level"]()} /> to <UiLabel name={m["zotero.prefs_log_level_debug.label"]()} />.

  </Step>
  <Step>
    ### Restart Zotero with logging enabled

    Go to **Help > Debug Output Logging** and click **Restart with Logging Enabled…**.

    <Callout type="warn" title="Do not use troubleshooting mode">
      A confirmation dialog appears. Do not select **Restart in Troubleshooting Mode**: that mode disables all add-ons, including the Companion, so no debug output would be captured.
    </Callout>

  </Step>
  <Step>
    ### Reproduce the problem

    As soon as Zotero restarts, reproduce the problem.

  </Step>
  <Step>
    ### Open the debug output viewer

    Go to **Help > Debug Output Logging > View Output**.

  </Step>
  <Step>
    ### Save the output

    In the viewer, go to **File > Save…**, choose Format: **Text Files**, and save the output.

    Do not use **Submit Output** or **Copy to Clipboard**. The full log file is needed.

  </Step>
  <Step>
    ### Upload the file

    Attach the saved text file to your bug report.

  </Step>
</Steps>

<Callout type="info" title="Reset the log level">
  Set the Companion's <UiLabel name={m["zotero.prefs_logging_level"]()} /> back
  to its previous value after collecting the output.
</Callout>

## See also

<Cards>
  <Card
    title="Settings"
    description={`Full reference for every ZotLit setting, including ${m.settings_page_advanced()}.`}
    href="/docs/reference/settings"
  />
  <Card
    title="Community"
    description="Where to file a bug report or ask a question."
    href="/community"
  />
</Cards>

---
title: "How to customize a template"
description: "Edit a literature note template in the web Template Workbench, save it to Obsidian, and update your notes."
introduced: "2.0.0"
updated: "2.0.0"
---

import { Steps, Step } from "fumadocs-ui/components/steps";
import { Card, Cards } from "fumadocs-ui/components/card";
import { Command } from "@/components/command";
import { SettingsPath } from "@/components/settings-path";
import { UiLabel } from "@/components/ui-label";
import { m } from "@/paraglide/messages.js";

Use the web Template Workbench to change a literature note's filename, properties, or body and preview the result.

## Open the web Template Workbench

<Steps>
  <Step>
    ### Select the template

    In Obsidian, open <SettingsPath />. On the <UiLabel name={m.settings_profile_document_name()} /> row, select <UiLabel name={m.settings_template_customize()} />.

    To edit the template used by an existing literature note, open that note and run <Command name={m.command_customize_note_template_name()} />. The same action is in the note's file menu. For another profile, open <UiLabel name={m.workbench_more_actions()} /> on its row at <SettingsPath page={m.settings_page_profiles()} /> and select <UiLabel name={m.profile_editor_customize()} />.

  </Step>
  <Step>
    ### Confirm the connection

    Select <UiLabel name={m.profile_editor_web_open()} /> for guided editing with sample data. Review the website, vault, paper, template, and access listed in the confirmation. Select <UiLabel name={m.modal_workbench_launch_open()} /> to open your browser.

    ZotLit turns on its Local Server if needed. The Workbench uses the active literature note's paper, or a sample item when no literature note is active. See [Local Server and browser access](/docs/concepts/template-workbench#local-server).

  </Step>
  <Step>
    ### Edit and preview

    Select a field to add Zotero data, or change the note in the editor. Check the rendered result as you edit. The <UiLabel name={m.workbench_start_here_heading()} /> strip shows these first steps and can be dismissed.

  </Step>
  <Step>
    ### Save to your vault

    Select <UiLabel name={m.workbench_save()} />. The first save of the built-in template creates its template document in your vault. Later saves update that file.

    Existing notes keep their content until you update them. In Obsidian, select <UiLabel name={m.notice_workbench_profile_saved_action()} /> in the save notice, or run <Command name={m.command_update_note_name()} /> for the current note.

  </Step>
</Steps>

The web editor supports Liquid templates. A template that requires Eta or JavaScript opens in Obsidian with a notice.

<UiLabel name={m.settings_template_customize()} /> remains available while web
access is off. The web choice asks for approval before enabling access. Select
<UiLabel name={m.modal_workbench_launch_remember()} /> to save your choice for
this vault on this device. Change it at
<SettingsPath
  page={m.settings_page_advanced()}
  setting={m.profile_editor_preference_name()}
/>
.

## Edit the document in Obsidian

On the <UiLabel name={m.settings_profile_document_name()} /> row, select <UiLabel name={m.settings_template_customize()} />, choose <UiLabel name={m.modal_workbench_launch_native()} />, and select <UiLabel name={m.modal_workbench_launch_open()} />. The Profile Editor opens in Obsidian. Opening the built-in template keeps it in memory; the first edit creates its document in your vault.

Edit the template in the Profile Editor. ZotLit saves your changes automatically. Run <Command name={m.command_update_note_name()} /> to apply them to the current literature note.

To return this template to its built-in form, select <UiLabel name={m.settings_profile_document_restore()} /> on the same row. ZotLit moves the file to Obsidian's configured trash. You can recover it from there; existing notes keep their content until updated.

## Customize a citation template

Citation templates have separate files. They control citations inserted into notes.

### Create an editable citation template file

<Steps>
  <Step>
    ### Open the citation template settings

    In Obsidian, open <SettingsPath page={m.settings_page_citations()} />. Under the <UiLabel name={m.settings_citation_templates_heading()} /> heading you see one row per citation template: <UiLabel name={m.settings_template_name_cite()} /> and <UiLabel name={m.settings_template_name_cite2()} />.

  </Step>
  <Step>
    ### Create the file

    On the row for the template you want to customize, click the <UiLabel name={m.settings_template_eject()} /> button (pencil-on-file icon). ZotLit copies the built-in default into your vault and opens it in the editor.

    A notice confirms the location: "Template saved to `{path}`."

    The file is created at `<template folder>/zotlit-<name>.liquid.md`, where `<template folder>` is the path configured in <SettingsPath page={m.settings_page_advanced()} setting={m.settings_template_folder_name()} /> (default: `templates`).

  </Step>
</Steps>

Each row is independent: create an editable file for one citation template and the other keeps its built-in default.

### Edit the citation template

Open the template file and change its content. Templates use Liquid syntax by default. The file is standard Markdown with embedded `{{ zt.field }}` expressions that pull data from the Zotero item.

To discover which fields are available and copy their paths, use the [Template Data Explorer](/docs/how-to/explore-template-data).

### See changes take effect

ZotLit watches the template folder and recompiles changed files immediately. No plugin reload is needed.

To apply your updated template to an existing literature note, run:

<Command name={m.command_update_note_name()} />

This re-renders the managed region using the current template. To update only frontmatter fields without touching the body, run:

<Command name={m.command_update_note_metadata_name()} />

### Restore a citation template

Two options return a template to its built-in default:

- <UiLabel name={m.settings_template_reset()} /> (rotate icon on the template
  row): replaces the file's contents with the built-in default while keeping the
  file in place. A confirm modal asks: "Replace '`{name}`' with the built-in
  default? Your changes will be lost."
- <UiLabel name={m.settings_template_delete()} /> (trash icon on the template
  row): moves the file to Obsidian's trash. With the file gone, ZotLit falls
  back to the built-in default automatically. The watcher picks up the deletion
  immediately.

### Switch a citation template to Eta (JavaScript)

After enabling the <UiLabel name={m.settings_template_js_enable_name()} /> gate, each template row shows a <UiLabel name={m.settings_template_language_tooltip()} /> dropdown with <UiLabel name={m.settings_template_language_liquid()} /> and <UiLabel name={m.settings_template_language_eta()} /> options. See [Enable JavaScript templates](/docs/how-to/enable-javascript-templates) for the opt-in procedure.

Selecting <UiLabel name={m.settings_template_language_eta()} /> replaces the file. It does not convert syntax between Liquid and Eta.

## See also

<Cards>
  <Card
    title="Install the ZotLit skill"
    description="Let an agent edit and test templates in this vault."
    href="/docs/install-skill"
  />
  <Card
    title="How templates work"
    description="The six template types, languages, filename matching, and the defaults model."
    href="/docs/concepts/how-templates-work"
  />
  <Card
    title="Explore template data"
    description="Browse available fields and copy paths into your template."
    href="/docs/how-to/explore-template-data"
  />
  <Card
    title="Configure frontmatter properties"
    description="Add custom YAML properties derived from Zotero data."
    href="/docs/how-to/configure-frontmatter-properties"
  />
  <Card
    title="Customize your annotations"
    description="Tutorial: guided worked example with before/after results."
    href="/docs/tutorial/customize-template"
  />
</Cards>

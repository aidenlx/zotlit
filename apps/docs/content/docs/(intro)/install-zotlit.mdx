---
title: "Install ZotLit"
description: "Install the ZotLit plugin in Obsidian."
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { VersionLedger } from "@/components/version-ledger";
import { ActionLink } from "@/components/action-link";
import { Steps, Step } from "fumadocs-ui/components/steps";
import { Callout } from "fumadocs-ui/components/callout";

ZotLit is a desktop-only Obsidian plugin. It runs on Windows, macOS, and Linux; mobile is not supported.

ZotLit v2 is currently in alpha. All v2 releases are GitHub pre-releases, so the Obsidian community directory does not serve v2 yet. To get v2, use the BRAT tab below.

## Install the plugin

<Tabs items={["BRAT", "Community directory"]}>
  <Tab value="BRAT">
    <VersionLedger app="obsidian" channel="pre-release" />

    BRAT (Beta Reviewers Auto-update Tester) is an Obsidian plugin that installs and auto-updates beta releases from GitHub. This is the recommended way to install ZotLit v2 during the alpha.

    <Steps>
      <Step>
        ### Install BRAT

        Open BRAT in the Obsidian plugin directory:

        <ActionLink href="obsidian://show-plugin?id=obsidian42-brat" kind="obsidian">Install BRAT</ActionLink>

        Enable it after installation. If you already have BRAT installed, skip to the next step.
      </Step>
      <Step>
        ### Add ZotLit as a beta plugin

        Click the link below to add ZotLit to BRAT automatically:

        <ActionLink href="obsidian://brat?plugin=aidenlx/zotlit" kind="obsidian">Add ZotLit to BRAT</ActionLink>

        Alternatively, open the command palette, run **BRAT: Add a beta plugin**, and enter `aidenlx/zotlit`.
      </Step>
      <Step>
        ### Enable ZotLit

        Go to **Settings > Community plugins** and enable **ZotLit**. You may need to refresh the plugin list first.

        BRAT keeps ZotLit up to date as new alpha releases ship. You do not need to reinstall manually.
      </Step>
    </Steps>

  </Tab>
  <Tab value="Community directory">
    <VersionLedger app="obsidian" channel="stable" />

    <Callout type="info" title="The community directory currently installs v1">
      The Obsidian community directory serves the stable v1 release, not the v2 alpha. During the alpha period, use the **BRAT** tab to install v2. The community directory will become the recommended path once v2 is promoted to the store.
    </Callout>

    You can find ZotLit in the Obsidian community plugin directory: [View ZotLit](https://community.obsidian.md/plugins/zotlit).

    <Steps>
      <Step>
        ### Open the plugin listing

        Click the link above, or open **Settings > Community plugins** in Obsidian, select **Browse**, and search for "ZotLit". You can also use this deep link:

        <ActionLink href="obsidian://show-plugin?id=zotlit" kind="obsidian">Show ZotLit in Obsidian</ActionLink>
      </Step>
      <Step>
        ### Install and enable

        Click **Install**, then **Enable**.
      </Step>
    </Steps>

  </Tab>
</Tabs>

## Next steps

ZotLit works best with the companion Zotero add-on. [Install the companion](/docs/install-companion) to connect your Zotero library, then follow the tutorial to [create your first literature note](/docs/tutorial/first-note).

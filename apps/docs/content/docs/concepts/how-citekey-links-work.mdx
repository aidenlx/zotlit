---
title: "How citation key styling works"
description: "How ZotLit recognizes Pandoc citation keys, styles and opens them in the editor, and renders them in reading view."
---

import { Callout } from "fumadocs-ui/components/callout";
import { Card, Cards } from "fumadocs-ui/components/card";

ZotLit reads the literal Pandoc citation keys you write — `@doe2024` and clusters such as `[see @doe2024, p. 3; @smith2020]` — and treats them as citations. **Citation key indexing** tracks them across your vault. **Citation key styling** is the editor and reading-view surface of that index: a recognized key is styled and clickable while you write, and shows its formatted citation text in reading view.

Both settings live in **Settings → ZotLit → Citations**. **Citation key indexing** is on by default and is the master switch; **Citation key styling** is on by default and takes effect only while indexing is on.

<Callout type="info" title="Upgrading from Citation Key Links">
  Citation Key Links is retired. ZotLit carries your stored toggle into
  **Citation key styling**, so clicking a citation key keeps working. The new
  feature no longer patches Obsidian editor internals, and it recognizes bare
  keys, not only bracketed ones.
</Callout>

## What gets recognized

ZotLit follows Pandoc's own citation grammar, so the text you write for Pandoc is the text ZotLit reads:

- `@key` — a bare author-in-text citation.
- `-@key` — the same citation with the author suppressed.
- `[@key]`, `[@key, p. 3]`, `[see @a, p. 3; @b]` — a citation cluster, with prefixes, locators, and several keys.
- `@{https://example.com/paper}` — a braced key, for a key that contains characters the bare form cannot carry.

Trailing punctuation is not part of a key, and text that only looks like a citation is left alone: `user@example.com` and a URL containing `@` stay plain text.

Text that Obsidian's editor classifies as code, math, a `%%` comment, frontmatter, or a URL is never read as a citation, so code samples and links never gain citation styling.

## What happens in the editor

A recognized key is styled like an internal link in Live Preview and Source mode, so you can see at a glance which text ZotLit reads as a citation. Click the key to open its literature note; `Ctrl`/`Cmd`-click or middle-click opens it in a new tab.

Styling comes from ZotLit's own editor decorations rather than from Obsidian's link machinery, so it applies to bare keys as well as bracketed ones.

## How click resolution works

ZotLit first looks for a literature note whose **Citation key property** holds that key. Exactly one match opens directly.

With no match or several matches, ZotLit finds the Zotero item by citation key in the **Default library**. It then checks the authoritative `zotero-key` identity: an existing literature note for that item opens, and only a key with no note at all creates one. This check prevents a duplicate when an existing note has a missing or different citation key property.

If no Zotero item carries the key, a notice appears: "No Zotero item with citation key `@{key}`." If the Zotero database is unavailable, ZotLit shows "Open the Zotero database to look up citation key `@{key}`."

For the full step-by-step details, see [Open a note from a citation key](/docs/how-to/create-and-open-notes#open-a-note-from-a-citation-key).

## What happens in reading view [#reading-mode]

Reading view replaces each citation with its formatted text, rendered in the style **References style** selects — the same text the references sidebar shows.

Without the Pandoc Engine installed, each key shows the `Creators (Year)` summary instead, and the brackets, prefixes, and locators you wrote stay as written.

Wikilinks in reading view are left untouched, so reading view stays plain Obsidian for them.

## Citation Key Property and Managed Frontmatter

The default Managed Frontmatter list includes a `citekey` field backed by `zt.citationKey`. You can edit, rename, or remove this ordinary managed field.

Set **Citation key property** to the property that stores citation keys in your Literature Notes. If the selected property is missing from Managed Frontmatter, ZotLit shows a notice. Citation key styling stays active, and existing unmanaged property values can still resolve notes.

Only a non-empty string value can match a citation key. The `zotero-key` property remains the system identity for Literature Notes.

## See also

<Cards>
  <Card
    title="Create and open literature notes"
    description="Open or create a literature note from a citation key click, the quick switcher, or the Zotero context menu."
    href="/docs/how-to/create-and-open-notes"
  />
  <Card
    title="Insert citations"
    description="Insert citation markers while typing or from the command palette."
    href="/docs/how-to/insert-citations"
  />
</Cards>

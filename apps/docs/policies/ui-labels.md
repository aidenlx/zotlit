# UI Labels

- Render ZotLit-owned command, setting, option, menu, button, and tooltip names through typed Paraglide `m.*()` calls from the shared catalog.
- Keep presentation such as the `ZotLit:` prefix, emphasis, and navigation separators at the component or MDX call site.
- Keep labels owned by Obsidian, Zotero, and other products as literal prose.
- Account for every handwritten ZotLit UI Label in changed prose before completion.

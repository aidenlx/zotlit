{
  "name": "@zotlit/zotero-types",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./dist/index.mjs",
    "./csl": "./dist/csl.mjs",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "generate:fields": "node scripts/generate-fields.ts && oxlint --fix && oxfmt",
    "generate:csl": "node scripts/generate-csl.ts && oxlint --fix && oxfmt",
    "typecheck": "tsc --noEmit -p tsconfig.lib.json && tsc --noEmit -p tsconfig.node.json",
    "lint": "oxlint"
  },
  "devDependencies": {
    "@types/node-25": "npm:@types/node@^25.6.2",
    "@zotlit/config": "workspace:*",
    "oxlint": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}

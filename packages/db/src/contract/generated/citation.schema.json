{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:zotlit:template-contract:v3:citation",
  "$comment": "Generated by scripts/generate-contract.ts - DO NOT EDIT.",
  "title": "zt (citation root)",
  "description": "The serialized `zt` data of the `citation` root.",
  "$ref": "#/$defs/CitationTemplateData",
  "$defs": {
    "CitationTemplateData": {
      "description": "The citation-template data root (`zt`): `citations[i].item === items[i]`.",
      "type": "object",
      "properties": {
        "variant": {
          "$ref": "#/$defs/CitationVariant",
          "description": "The gesture this Citation was requested with."
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TemplateCiteItemData"
          },
          "description": "The cited items bare, in Citation order."
        },
        "citations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CitationTemplateItem"
          },
          "description": "The Citation Items: each pairs a cited item with the citation-scoped properties the Pandoc Citation formatter reads."
        }
      },
      "required": ["variant", "items", "citations"],
      "additionalProperties": false
    },
    "CitationVariant": {
      "oneOf": [
        {
          "const": "main",
          "description": "Enter in the suggester, the insert modal, and an annotation's `citation`."
        },
        {
          "const": "alt",
          "description": "Shift+Enter, or a trailing `/` in the suggester query."
        }
      ],
      "description": "The gesture a Citation was requested with, handed to the Citation Template as `zt.variant`: `\"main\"` for Enter, `\"alt\"` for Shift+Enter or a trailing `/` in the suggester query. The enum names the gesture alone — the Citation Template decides what each variant renders."
    },
    "TemplateCiteItemData": {
      "description": "A cited item's data, in the same template vocabulary as the note root and narrowed to the fields both a live Zotero item and a note's embedded CSL-JSON snapshot supply. Vault and library context — tags, dates-added/modified, library identity, and the note resolvers — stays on the note root, which the embedded snapshot cannot express. An unresolved reference becomes a stub with every field null, so a citation template never null-checks the item itself.",
      "type": "object",
      "properties": {
        "itemType": {
          "type": ["string", "null"],
          "description": "Zotero item type, e.g. `\"journalArticle\"`, `\"book\"`."
        },
        "creators": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TemplateCreator"
          },
          "description": "Every creator on the item, in Zotero's own order."
        },
        "primaryCreatorType": {
          "type": ["string", "null"],
          "description": "The creator role Zotero treats as primary for this item type."
        },
        "title": {
          "type": ["string", "null"],
          "description": "Item title."
        },
        "abstract": {
          "type": ["string", "null"],
          "description": "Abstract text; the CSL-inspired rename of Zotero's `abstractNote`."
        },
        "containerTitle": {
          "type": ["string", "null"],
          "description": "Journal or container title; the CSL-inspired rename of `publicationTitle`."
        },
        "citationKey": {
          "type": ["string", "null"],
          "description": "Citation key, from Better BibTeX or Zotero's native citation key."
        },
        "citekey": {
          "type": ["string", "null"],
          "description": "Alias for `citationKey`; both stay accessible on the item."
        },
        "date": {
          "anyOf": [
            {
              "$ref": "#/$defs/ItemDateYMD"
            },
            {
              "$ref": "#/$defs/ItemDateYearMonth"
            },
            {
              "$ref": "#/$defs/ItemDateYear"
            },
            {
              "$ref": "#/$defs/ItemDateText"
            },
            {
              "type": "null"
            }
          ],
          "description": "Publication date, parsed from Zotero's multipart `date` field."
        },
        "shortTitle": {
          "type": ["string", "null"],
          "description": "Short title."
        },
        "DOI": {
          "type": ["string", "null"],
          "description": "Digital Object Identifier."
        },
        "url": {
          "type": ["string", "null"],
          "description": "Item URL."
        },
        "ISBN": {
          "type": ["string", "null"],
          "description": "ISBN."
        },
        "ISSN": {
          "type": ["string", "null"],
          "description": "ISSN."
        },
        "volume": {
          "type": ["string", "null"],
          "description": "Volume."
        },
        "issue": {
          "type": ["string", "null"],
          "description": "Issue."
        },
        "pages": {
          "type": ["string", "null"],
          "description": "Page range."
        },
        "publisher": {
          "type": ["string", "null"],
          "description": "Publisher."
        },
        "place": {
          "type": ["string", "null"],
          "description": "Place of publication."
        },
        "edition": {
          "type": ["string", "null"],
          "description": "Edition."
        },
        "language": {
          "type": ["string", "null"],
          "description": "Language, verbatim as Zotero stores it, e.g. `\"en-US\"`, `\"English\"`."
        },
        "extra": {
          "anyOf": [
            {
              "$ref": "#/$defs/ItemExtra"
            },
            {
              "type": "null"
            }
          ],
          "description": "Parsed view of Zotero's free-text `extra` field; `null` when empty."
        }
      },
      "required": [
        "itemType",
        "creators",
        "primaryCreatorType",
        "title",
        "abstract",
        "containerTitle",
        "citationKey",
        "citekey",
        "date",
        "shortTitle",
        "DOI",
        "url",
        "ISBN",
        "ISSN",
        "volume",
        "issue",
        "pages",
        "publisher",
        "place",
        "edition",
        "language",
        "extra"
      ],
      "additionalProperties": {
        "description": "Additional Zotero fields beyond the explicitly typed ones above."
      }
    },
    "TemplateCreator": {
      "description": "One creator of an item, in the template vocabulary. Coerces to `TemplateCreator.fullName` in string contexts.",
      "type": "object",
      "properties": {
        "family": {
          "type": "string",
          "description": "Family / last name; empty for institutional creators."
        },
        "given": {
          "type": "string",
          "description": "Given / first name; empty for institutional creators."
        },
        "literal": {
          "type": ["string", "null"],
          "description": "Full name for institutional / single-name authors (Zotero `fieldMode=1`); `null` for personal names."
        },
        "role": {
          "type": "string",
          "description": "Zotero creator type: `\"author\"`, `\"editor\"`, `\"translator\"`, etc."
        },
        "fullName": {
          "type": "string",
          "description": "`literal` for institutional creators, `\"given family\"` for personal names."
        }
      },
      "required": ["family", "given", "literal", "role", "fullName"],
      "additionalProperties": false
    },
    "ItemDateYMD": {
      "description": "Full Y/M/D date parsed into a `Temporal.PlainDate`.",
      "type": "object",
      "properties": {
        "kind": {
          "const": "date",
          "description": "Discriminator for the full-precision variant."
        },
        "value": {
          "type": "string",
          "description": "The parsed calendar date."
        },
        "year": {
          "type": "number",
          "description": "Numeric year."
        },
        "month": {
          "type": "number",
          "description": "Month, 1-12."
        },
        "day": {
          "type": "number",
          "description": "Day of month."
        },
        "raw": {
          "type": "string",
          "description": "The stored Zotero `date` value verbatim, retained for round-tripping."
        }
      },
      "required": ["kind", "value", "year", "month", "day", "raw"],
      "additionalProperties": false
    },
    "ItemDateYearMonth": {
      "description": "Year and month only; `Temporal.PlainYearMonth` (no day).",
      "type": "object",
      "properties": {
        "kind": {
          "const": "yearMonth",
          "description": "Discriminator for the year-and-month variant."
        },
        "value": {
          "type": "string",
          "description": "The parsed year and month."
        },
        "year": {
          "type": "number",
          "description": "Numeric year."
        },
        "month": {
          "type": "number",
          "description": "Month, 1-12."
        },
        "day": {
          "type": "null",
          "description": "Always `null` — this variant carries no day."
        },
        "raw": {
          "type": "string",
          "description": "The stored Zotero `date` value verbatim, retained for round-tripping."
        }
      },
      "required": ["kind", "value", "year", "month", "day", "raw"],
      "additionalProperties": false
    },
    "ItemDateYear": {
      "description": "Year only. Also covers Zotero's `01 2003` misparse where the user-string day is meaningful but month is missing (the spurious day is dropped since no `Temporal` type fits \"year + day without month\").",
      "type": "object",
      "properties": {
        "kind": {
          "const": "year",
          "description": "Discriminator for the year-only variant."
        },
        "value": {
          "type": "null",
          "description": "Always `null` — no `Temporal` type holds a bare year."
        },
        "year": {
          "type": "number",
          "description": "Numeric year."
        },
        "month": {
          "type": "null",
          "description": "Always `null` — the month is missing or unparseable."
        },
        "day": {
          "type": "null",
          "description": "Always `null` — this variant carries no day."
        },
        "raw": {
          "type": "string",
          "description": "The stored Zotero `date` value verbatim, retained for round-tripping."
        }
      },
      "required": ["kind", "value", "year", "month", "day", "raw"],
      "additionalProperties": false
    },
    "ItemDateText": {
      "description": "Unparseable / text-only: the year is missing or the SQL prefix is absent.",
      "type": "object",
      "properties": {
        "kind": {
          "const": "text",
          "description": "Discriminator for the text variant."
        },
        "value": {
          "type": "null",
          "description": "Always `null` — nothing parsed, so there is no `Temporal` value."
        },
        "text": {
          "type": "string",
          "description": "The renderable user portion, or the entire raw value when non-multipart."
        },
        "year": {
          "type": ["number", "null"],
          "description": "Year extracted from `ItemDateText.text` when it holds a standalone 4-digit year in the 1000-2999 range, else `null`."
        },
        "month": {
          "type": "null",
          "description": "Always `null` — the text variant resolves no month."
        },
        "day": {
          "type": "null",
          "description": "Always `null` — the text variant resolves no day."
        },
        "raw": {
          "type": "string",
          "description": "The stored Zotero `date` value verbatim, retained for round-tripping."
        }
      },
      "required": ["kind", "value", "text", "year", "month", "day", "raw"],
      "additionalProperties": false
    },
    "ItemExtra": {
      "description": "Best-effort structured view of Zotero's `extra` field. Plain data so it is directly indexable in Liquid, Eta, and JS.\n\n`toString` is non-enumerable, so a spread, `JSON.stringify`, or `Object.keys` sees the three data properties alone.",
      "type": "object",
      "properties": {
        "raw": {
          "type": "string",
          "description": "Original field text verbatim, for a full round-trip."
        },
        "fields": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "First value per key — the common-case lookup, e.g. `zt.extra.fields[\"tex.mendeley-tags\"]`. First-wins: a repeated key keeps its first occurrence."
        },
        "lines": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ExtraLine"
          },
          "description": "Every source row in order, the source of truth `ItemExtra.fields` derives from. Scan it to recover every value of a repeated key."
        }
      },
      "required": ["raw", "fields", "lines"],
      "additionalProperties": false
    },
    "ExtraLine": {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "raw": {
              "type": "string",
              "description": "Verbatim source line."
            },
            "key": {
              "type": "string",
              "description": "Parsed key, trimmed."
            },
            "value": {
              "type": "string",
              "description": "Parsed value, trimmed; always non-empty on a pair row."
            }
          },
          "required": ["raw", "key", "value"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "raw": {
              "type": "string",
              "description": "Verbatim source line."
            },
            "key": {
              "type": "null",
              "description": "Always `null` — prose, blank, and empty-value rows carry no key."
            }
          },
          "required": ["raw", "key"],
          "additionalProperties": false
        }
      ],
      "description": "One source row of an Extra field, in document order: a parsed pair (`key` plus trimmed `value`) or a non-pair text/blank row (`key: null`). `raw` is the verbatim line so interleaved prose is never dropped.\n\nA line splits on its first `:` or `=`, and is a pair only when the key starts with an ASCII letter and otherwise holds ASCII letters, digits, spaces, dots, hyphens, or underscores. A line that does not match, and one whose value is empty, becomes a text row.\n\nA non-enumerable `toString` returns `raw`, as on the extra field itself."
    },
    "CitationTemplateItem": {
      "description": "One cited item within a Citation: the pure item data plus the citation-scoped properties, which never live on the item. Exposed to the Citation Template as `zt.citations`, with `zt.items` the same items bare.",
      "type": "object",
      "properties": {
        "item": {
          "$ref": "#/$defs/TemplateCiteItemData",
          "description": "Never null; a stub with null fields when the ref is unresolved."
        },
        "locator": {
          "type": ["string", "null"],
          "description": "Pinpoint reference, e.g. `\"62\"`; `null` when the Citation names none."
        },
        "label": {
          "type": ["string", "null"],
          "description": "Raw CSL locator label, e.g. `\"page\"`."
        },
        "labelShort": {
          "type": "string",
          "description": "Pandoc-style abbreviation, e.g. `\"p.\"`; `\"page\"`/absent → `\"p.\"`."
        },
        "suppressAuthor": {
          "type": "boolean",
          "description": "The Citation suppresses the author's name; `false` by default."
        },
        "prefix": {
          "type": ["string", "null"],
          "description": "Text rendered before the Citation, e.g. `\"see\"`."
        },
        "suffix": {
          "type": ["string", "null"],
          "description": "Text rendered after the Citation, e.g. `\"for a review\"`."
        }
      },
      "required": [
        "item",
        "locator",
        "label",
        "labelShort",
        "suppressAuthor",
        "prefix",
        "suffix"
      ],
      "additionalProperties": false
    }
  }
}

/**
 * `libraryID` of the user's personal library, which Zotero always bootstraps as
 * row 1 (`type = 'user'`); group libraries take subsequent ids.
 *
 * @see https://github.com/zotero/zotero/blob/9.0.3/chrome/content/zotero/xpcom/schema.js#L2786
 */
export const USER_LIBRARY_ID = 1;
